# Placeholder for Windows binary
# The actual MeowthBridge.exe will be placed here during the build process
# by GitHub Actions or manual compilation.

To build manually:
  dotnet publish src/MeowthBridge -c Release -r win-x64 --self-contained -p:PublishSingleFile=true -o src/meowth/binaries/windows
